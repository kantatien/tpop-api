syntax = "proto3";
package example;

service ExampleService {
  rpc ExampleMessage(EmptyRequest) returns (MessageResponse);
}

message EmptyRequest{
}

message MessageResponse{
  int32 code = 1;
  bool success = 2;
  string message = 3;
  string timestamp = 4;
}
