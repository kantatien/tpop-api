const config = {
    // kafkaConfig: require("./kafka.config"),
    // redisConfig: require('./redis.config'),
    grpcEndpointConfig: require('./grpcendpoint.config'),
    // sentryConfig: require('./sentry.config'),
    // oauthConfig: require('./oauth.config'),
    graphqlConfig: require('./graphql.config'),
    // healthyConfig: require('./healthy.config'),
  };
  
  module.exports = config;
  